# Boini AI Report — 프로젝트 가이드

## 개발 워크플로우: RDD (Review Driven Development)

기능 구현이나 코드 수정 작업은 아래 루프를 따른다.

1. **구현/수정** — 요청받은 기능을 구현하거나 코드를 수정한다.
2. **리뷰 호출** — 구현/수정 직후 반드시 `code-reviewer` 서브에이전트를 호출한다.
3. **수정** — 리뷰어가 반환한 `[BLOCKER]` / `[WARN]` 지적을 수정한다.
4. **재리뷰** — 다시 `code-reviewer` 를 호출한다.
5. **반복** — `APPROVED` 가 나올 때까지 3~4를 반복한다.

### 규칙
- **`APPROVED` 전에는 작업을 완료로 간주하지 않는다.**
- `[NIT]` 은 권고일 뿐이며 `APPROVED` 를 막지 않는다. (반영은 선택)
- 리뷰어 지적이 **명백한 오탐**이면 맹목적으로 따르지 말고, **근거를 남기고** 넘어간다.
- **같은 지적으로 3회 이상 왕복**하면 멈추고 사람에게 판단을 요청한다.

> 참고: `.claude/agents/code-reviewer.md` 는 리뷰 전용(수정 권한 없음) 서브에이전트다.
> 세션 시작 시 로드되므로, 자동 위임에 안 잡히면 Claude Code 재시작이 필요할 수 있다.

### 테스트 규칙
- 로직/동작을 추가하거나 바꾸는 구현에는 **항상 테스트 코드를 함께 작성**한다.
  특히 엣지 케이스(빈 입력, 경계값, 차원/타입 불일치 등)는 테스트로 고정한다.
- 버그를 고칠 때는, 그 **버그를 재현하는 테스트를 먼저 추가**해 회귀를 방지한다.
- **테스트도 `code-reviewer` 리뷰 대상에 포함**한다. (무의미하게 항상 통과하는 테스트,
  실제 로직을 우회하는 mock 등을 걸러내기 위함)
- 예외: 문서/주석/설정값/네이밍 등 **동작을 바꾸지 않는 사소한 변경**은 테스트를 요구하지 않는다.
- **완료 기준: 관련 테스트가 전부 통과 + `code-reviewer` APPROVED. 둘 다 충족해야 완료로 본다.**

## 배포 노트

- **임베딩 모델 교체 시 클러스터 상태(`room:*:clusters`) 초기화 필수.** centroid 차원이
  바뀌면 기존 상태와 코사인 계산 시 차원 불일치가 난다. (코드에도 차원 불일치 시 자동
  리셋 가드가 있지만, 배포 시 Redis 상태를 비우는 것을 기본 절차로 둔다.)
